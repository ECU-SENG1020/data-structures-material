# Building a Custom List data structure
1. What happens when append() is called the first time? 
```python
my_custom_list = CustomList()
my_custom_list.append('abc')
```
2. What happens on the fourth time append() is called?
```python
my_custom_list = CustomList()
my_custom_list.append('abc')
my_custom_list.append('def')
my_custom_list.append('ghi')
my_custom_list.append('jkl')
```
3. What are the edge cases to consider for the append() method?

4. What happens when you ask for the length of my_custom_list?
```python
num_elements =  len(my_custom_list)
```
5. What are the edge cases to consider for the __len__() method?

6. What is the first parameter of all instance methods?

7. How do I dictate the output from the following code?
```python
print(my_custom_list)
```
8. What is a dunder attribute?

9. What is the most important node in a Singly Linked List?





