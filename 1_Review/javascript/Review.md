# JavaScript Review Questions

1. What is the difference between `console.log` and `return`?
2. What is the difference between an `Array` and an `Object`?
3. When would you use a `Map` instead of an `Object`?
4. When would you use a `Set`?
5. What does `import { name } from './file.js'` mean?
6. What does `export` do?
7. What is `this` inside a class method?
8. How do `map` and `filter` relate to Python list comprehensions?
9. What is a generator function (`function*`)?
10. What does `Symbol.iterator` enable?
