# Julia Review Questions

1. What does `println` do?
2. What is the difference between a `Vector` and a `Tuple`?
3. How do you create a `Dict`? A `Set`?
4. What does `include("file.jl")` do?
5. What does `using .MyModule: Name1, Name2` mean?
6. What does the `!` at the end of a function name usually mean?
7. How do you make a custom type printable with `println(x)`?
8. How do comprehensions work in Julia? (Example: `[x^2 for x in 1:5]`)
9. What is the difference between a generator expression and `collect(...)`?
