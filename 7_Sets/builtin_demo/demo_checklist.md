1. Creating sets - Different initialization methods (empty, literals, constructors)
1. Uniqueness - duplicate removal and behavior when inserting duplicates
1. Element types - only hashable elements allowed; behavior with mutable types
1. Adding elements - `add`, `push` (language differences) and `union`/`update`
1. Removing elements - `remove`, `discard`/`delete`, `pop`, `clear`
1. Membership tests - `in`, `has`, `contains`
1. Iterating - order is not guaranteed; iteration patterns
1. Set algebra - union, intersection, difference, symmetric difference
1. In-place vs returning operations - `update`/`intersection_update` etc.
1. Subset and superset - `issubset`, `issuperset`, proper subset checks
1. Disjointness - `isdisjoint` / equivalent
1. Conversions - set <-> list/array/vector, removing duplicates from sequences
1. Frozen/immutable sets - `frozenset` and hashability of sets
1. Comprehensions - set comprehensions / generators
1. Performance notes - typical complexity for membership and common operations
1. Language-specific notes - WeakSet (JS), Set element hashing (Julia/Python)
