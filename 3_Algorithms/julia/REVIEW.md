# Algorithms Review (Julia)

1. What is the difference between `linear_search` and `binary_search`?
2. Why must the list be sorted for binary search?
3. What is the base case in a recursive function?
4. What does `time_ns()` measure?
5. Why do selection sort and insertion sort take about $O(n^2)$ time?
6. Why does merge sort take about $O(n\log n)$ time?
7. Why does Julia use 1-based indexing, and how does that affect these algorithms?
