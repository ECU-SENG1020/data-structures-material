# Algorithms Review (JavaScript)

1. What is the difference between linear search and binary search?
2. Why must the array be sorted for binary search?
3. What is the base case in a recursive function?
4. Why do selection sort and insertion sort take about O(n^2) time?
5. Why does merge sort take about O(n log n) time?
6. What is memoization, and how does it help Fibonacci?
